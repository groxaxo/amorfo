version: '3.8'

services:
  morpheus:
    build: .
    container_name: morpheus
    network_mode: host
    privileged: true
    volumes:
      - ./settings.yml:/app/settings.yml
    environment:
      - PULSE_SERVER=unix:${XDG_RUNTIME_DIR}/pulse/native
    devices:
      - /dev/snd:/dev/snd
